> 2026-09-22 16시 이후 갱신: 사용자가 Apple 입력 한도를 위해 데이터를 줄이는 작업을 중단하고 이전 DeepSeek 복귀를 선택했다. 현재 생성·판정은 Command Code DeepSeek Flash이며 Docker/실제 화면 검증 완료. 아래 Apple 계속 구현 단계는 재개하지 말고 [최신 검토](docs/APPLE_USABILITY_REVIEW_2026-09-22.md)를 먼저 읽는다.

# Handoff — 2026-09-22 13:53 (Apple 온디바이스 판정 최적화·판정 모델 선택)

사용자 요청으로 작업을 잠시 멈추고 현재 상태와 남은 일을 기록한다. 브랜치 `codex/integrated-rag-catalog`, 마지막 커밋 977ccef. **오늘 오후 변경 20개 파일은 모두 미커밋**이다.

## 지금까지 한 일

- 판정 모델 옵션 실측(전환 전 DeepSeek 기준선, Apple 온디바이스 1차, glm-5.3-flashx): `docs/verification/judge-switch-2026-09-22/`. 1차 Apple 판정은 정답 대부분을 차단했고 glm은 속도 이점 없이 형식 불량 2/10이었다.
- 사용자 지시 "DeepSeek·glm은 선택지로 남기고 Apple 온디바이스에 맞춰 최적화"에 따라 두 가지를 구현했다.
  1. **Apple 판정 분해 방식** (`extensions/code_agent/apple_judge.py`): 원본 판정 프롬프트에서 근거·답변 JSON을 꺼내 근거는 한국어 표찰 문장으로 다시 적고 답변은 줄 단위 주장으로 나눈다. `메시지코드:` 블록의 `문구/메뉴/노출 조건` 줄은 DB 원문과 문자 비교(기계), 자유 서술 줄만 온디바이스 모델에 구조화 출력으로 묻는다. 뒷받침되지 않는 주장이 하나라도 있으면 UNFAITHFUL/FAIL. 호스트 하네스(실제 형식 프롬프트, 변형 10종 × 3회) 결과 정답 통과·오답 차단 모두 만족, 기계 비교만으로 끝나는 정답은 모델 호출 0회.
  2. **판정 모델 선택 UI** (`extensions/code_agent/static/index.html`·`app.js`, `ai_config.py`, `provider.py`): 관리자 AI 설정의 "답변 검증 AI"에서 `생성 모델과 동일 / 같은 공급자의 다른 모델(모델명) / 이 Mac의 Apple 온디바이스 모델`을 고른다. `ai.json`의 `judge`로 저장되고 `.env`의 `CODE_LLM_JUDGE_*`보다 우선한다. Apple 항목은 브리지 `/health`가 준비 상태일 때만 저장(아니면 503).
- 브리지(`extensions/apple_bridge/bridge.py`, `applefm.swift`)와 `manage.py` 연동: macOS에서 `start`가 브리지를 함께 띄운다(모델 불가 시 선택 불가로 두고 계속, env 강제 시 중단).
- 테스트: 확장 pytest **283개**, 설치 unittest **77개**, native HTTP **10개** 통과(13:40경, haiku 위임 실행). 신규는 `integration_tests/test_apple_judge.py`(17), `test_judge_selection.py`(3), `tests/test_apple_bridge.py`(5).
- 문서: AI_SETTINGS(Apple 절·화면 절차 4단계), README 17행, INTEGRATION 마지막 절, CLAUDE.md, STATUS(테스트 수·판정 옵션 행), TEST_REPORT 마지막 항목(1차 실측까지만 반영), `docs/verification/judge-switch-2026-09-22/README.md`(1차 실측까지).

## 현재 실행 상태 (13:53 확인)

- 새 API 이미지로 재배포 완료. 컨테이너의 `apple_judge.py`·`ai_config.py`·`static/app.js` sha256이 작업 트리와 일치.
- `.env`에는 판정 설정이 없다(`CODE_LLM_JUDGE_*` 비움). AI 설정 API는 `judge: {mode: same, source: env}`, `apple_judge_state: ready`를 반환한다. 즉 **지금은 DeepSeek(생성 모델)가 판정**하고, Apple은 화면에서 선택 가능한 상태다.
- Apple 브리지 실행 중(pid는 `python3 extensions/apple_bridge/bridge.py status`로 확인). `.env`에 `CODE_APPLE_BRIDGE_TOKEN` 있음.
- 재배포의 `docker compose up --build --wait`가 이번에도 오류 문구 없이 종료코드 1을 반환했으나 모든 서비스 정상(1차와 동일 현상, 원인 미상).

## 해야 할 일 (순서대로)

1. **화면에서 Apple 판정 선택 확인**: 브라우저로 `http://localhost:3500/codes` → AI 설정 → 답변 검증 AI에서 "이 Mac의 Apple 온디바이스 모델" 선택 후 저장. 저장 성공과 `GET /api/code-catalog/status?namespace=demo`의 `llm.judge_provider: apple` 확인. 스크린샷을 `docs/verification/judge-switch-2026-09-22/`에 남길 것.
2. **실제 턴 재계측(Apple 2차)**: `python3 docs/verification/judge-switch-2026-09-22/measure_turns.py after-apple-v2 docs/verification/judge-switch-2026-09-22/after-apple-v2.json`. 기대: 10건 중 무관 질의 포함 정상 응답, 정답 1위 유지, 판정 시간 0~3초.
3. **실제 형식 오답 벤치(Apple 2차)**: 일회용 컨테이너에서 `judge_bench_real.py`를 `CODE_LLM_JUDGE_PROVIDER=apple`로 실행(명령은 아래). 기대: 정답 2종 통과, 오답 4종 차단.
   ```bash
   S=/private/tmp/claude-501/-Users-apple-Documents-ChatGPT-code-management/c4e0ce19-8c5b-4a60-a03c-21a74115d728/scratchpad
   docker run --rm --add-host host.docker.internal:host-gateway --env-file $S/bench.env -v "$PWD/extensions:/ext:ro" -v "$PWD/docs/verification/judge-switch-2026-09-22/judge_bench_real.py:/bench.py:ro" -e PYTHONPATH=/ext:/app code-management-full-api:75661c6 python /bench.py apple-v2 3
   ```
   `bench.env`는 `.env`의 CODE_*·COMMANDCODE_*·LANGFUSE 키를 따옴표 없이 정리한 파일(스크래치패드에 있음, 없으면 `manage.load_env`로 다시 생성).
4. **선택지 왕복 확인**: 화면에서 "같은 공급자의 다른 모델"에 `z-ai/glm-5.3-flashx`를 넣어 저장 → 상태 API `judge_model` 확인 → 다시 "생성 모델과 동일"로 복귀. 최종 상태는 사용자 결정에 맡긴다(기본은 동일).
5. **문서 마감**: TEST_REPORT에 2차 실측 항목 추가(분해 방식·UI 선택·수치), `judge-switch` README에 2차 표 추가, STATUS 판정 행 갱신, AI_SETTINGS "확인한 범위" 한 줄. 문체 검사 지적 중 기존 행(INTEGRATION 130행, TEST_REPORT 40·89·103·154~208행)은 이전 커밋 원문이라 그대로 둔다.
6. **메모리 갱신**: `~/.claude/projects/-Users-apple-Documents-ChatGPT-code-management/memory/rag-review-handoff-2026-09-22.md`에 2차 결과와 최종 배포 상태를 반영.
7. **커밋 여부**는 사용자에게 확인(미커밋 20개 파일, `.env`·`.local/`은 gitignore).

## 주의

- `upstream/` 수정 금지, `CODE_AUTH_MODE=local` 유지, `docker compose down -v` 금지.
- 판정 생략·캐시·조용한 대체 금지. Apple 판정은 브리지 불통 503, 브리지 오류·형식 불량 502로만 실패한다.
- 기존 `docs/TEST_REPORT.md`의 1차 Apple 결론("켜지 않기를 권한다")은 분해 방식 이전의 결과다. 2차 실측 뒤 그 항목 끝에 후속 항목을 덧붙이고 1차 기록은 지우지 않는다.
- Apple 판정에서 `bad_numeric_60s` 변형은 모델 호출 전에 수치 검증 단계가 차단한다(정상).
- macOS에는 `timeout` 명령이 없다. Bash 도구의 timeout 파라미터를 쓸 것.
- sprag 위임 후보 R1(명령 실행 유형 → sonnet, 프로젝트 범위 제안)은 미결. 등록은 `sprag harness promote R1 --global|--project`.

## 다음 세션 재시작 프롬프트

```
HANDOFF-2026-09-22-1353.md를 읽고 "해야 할 일" 1번부터 이어서 진행해.
```


## 2026-09-22 최종 범위: Apple 연결만 유지

사용자의 “그냥 연결만” 요청에 따라 Apple 전용 검색 관련성 필터, 상위 1건 입력 제한, 고정 답변 대체, 대화 프롬프트 재작성을 제거했다. 기존 검색·프롬프트·검증 흐름을 유지하고 대화 공급자와 답변 검증 공급자를 `apple/on-device`로 연결했다. multilingual-e5-base(768→1536 패딩), PGVector, Nori, RRF, 상위 12건 리랭킹은 변경하지 않았다.

API·worker·beat 재배포 완료. 컨테이너 내 연결 관련 파일 8개의 해시가 작업 트리와 일치한다. 배포된 `RoutingLLM`을 통한 실제 Apple 응답 “안녕하세요! 어떻게 도와드릴까요?”를 확인했다. 확장/API 검사 294개, 설치 unittest 77개, 대역 기반 native HTTP 10개 및 JS 구문 검사 통과. 이는 연결 검증이며 긴 카탈로그 프롬프트의 답변 품질·성공률 검증을 뜻하지 않는다. 이전 `apple-chat-*` 실험 결과는 최종 구현의 품질 수치로 사용하지 않는다. 관련 로그는 `docs/verification/judge-switch-2026-09-22/*connection-only*`에 있다. 커밋하지 않았다.

## 2026-09-22 14:10 추가: 설정 화면 재배치

사용자가 "답변 검증 AI와 검색이 왜 생겼는지 모르겠다"고 하여 AI 설정 화면을 질문 처리 순서로 다시 배치했다. 상단에 "① 후보 찾기(검색) → ② 답변 작성(대화 AI) → ③ 답변 검증(원문 대조) → 저장" 안내를 넣고, 세 칸을 그 순서로 두고 각 칸 아래 한 줄 설명을 붙였다. 검색 상태 문구는 고른 방식에 맞게 바뀌고(키워드 선택 시 "임베딩 모델 없이…"), 판정 기본 선택 문구는 "답변 작성 AI(모델명)가 검증도 맡습니다"로 바꿨다. 요소 id는 그대로다. `node --check`·id 중복 검사·native HTTP 통과. 재배포를 시작했다(`.local/deploy-settings-ui-2026-09-22.log`). 남은 확인: 배포 뒤 브라우저에서 화면 순서·문구 확인과 스크린샷.

## 2026-09-22 14:30 추가: 설정 화면 원복 (사용자 결정)

사용자가 "원래처럼 복구해"라고 하여 AI 설정 화면을 원래의 두 칸(대화 AI·검색)으로 되돌렸다. 제거한 것: "답변 검증 AI" 칸, 상단 처리 순서 안내, 칸 번호·설명문, 저장형 판정 선택(`ai.json.judge`, `judge_mode`/`judge_model`, `judge_setting`, `JUDGE_MODES`). 남긴 것: 다른 세션이 넣은 대화 AI의 Apple 옵션과 그 상태 문구, `judge_apple_status()`(설정 API의 `apple_judge_state`·`apple_judge_available`), `.env` 기반 판정 선택(`CODE_LLM_JUDGE_*`), 문장 분해 Apple 판정(`apple_judge.py`), 브리지. `provider.judge_options()`는 env만 읽고 대화 AI가 Apple이면 판정도 Apple로 보낸다(다른 세션의 규칙 유지). `test_judge_selection.py`는 브리지 상태 검사와 "판정 항목은 이제 미지원 필드" 검사만 남겼고, `test_apple_chat.py`의 `judge_mode` 인자를 제거했다. 초점 검사 122개·native HTTP 통과, 전체 회귀는 haiku 위임으로 실행 중. 재배포 시작(`.local/deploy-settings-restore-2026-09-22.log`). 배포 뒤 화면 확인·스크린샷이 남았다. 저장된 `ai.json`의 옛 `judge` 키는 다음 저장 때 사라지며 읽기에는 영향이 없다.

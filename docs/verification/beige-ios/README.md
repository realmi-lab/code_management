# Beige / iOS-style workspace verification

Scope: catalogue workspace HTML/CSS and the /codes Next.js wrapper. Original upstream files were not edited. The verified upstream tree was checked before the Docker build.

Changes: warm ivory and stone surfaces, system typography, restrained blue actions, segmented navigation, grouped example list, removed placeholder specimen, restrained motion, viewport-contained conversation/input area, and route-scoped matching parent shell. Approval cancel skips required-field validation.

Checks:
- Python integration suite: 109 passed (test doubles where specified in the suite).
- Installer unittest suite: 58 passed.
- Native localhost HTTP: 10 checks passed with synthetic authentication/model.
- Chromium HTTP UI: 12 checks passed, no JavaScript errors; synthetic auth/model and temporary test database.
- JavaScript syntax check passed.
- Production Next.js build and backend Docker build passed. Source CSS brace check passed; Next build also compiled the wrapper stylesheet and TSX.
- Rebuilt rag-api and rag-frontend were started on the existing local compose deployment.
- Actual authenticated /codes route inspected in the Codex browser at 1440x1000, 820x1000, 390x844, and 360x780. The mobile first screen shows the composer and send control. Catalogue empty state, AI settings form, and expanded comparison fields inspected. No production records or AI settings were changed by this design work.

Limits: no physical iPhone or software-keyboard test; no claim of real semantic-search quality. The existing upstream system status shows partial degradation; this visual change does not resolve that backend status. The current production catalogue is empty.

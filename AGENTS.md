# 다음 작업자의 기준

README.md → docs/INTEGRATION.md → docs/TEST_REPORT.md를 먼저 읽는다.

## 사용자 확정 금지 사항 — 관리자 로그인 메뉴 복원 금지

- **`UrstoryRAG / 관리자 콘솔에 로그인하세요` 화면·메뉴·진입 링크를 절대 복원하거나 다시 노출하지 않는다.** 문구만 바꾸어 같은 관리자 로그인 메뉴를 되살리는 것도 금지한다.
- 로그인·관리자 비밀번호 입력 없이 `/codes`로 진입하는 현재 로컬 사용 흐름을 유지한다. 원본 동기화·기능 복원·디자인 변경·오류 처리 과정에서 로그인 화면을 다시 넣거나 로그인 화면으로 우회시키지 않는다.
- 이 지시는 아래 원본 로그인·화면 보존 원칙보다 우선한다. 고정 upstream에 로그인 소스가 남아 있거나 선택형 JWT 구현이 존재한다는 이유로 해당 메뉴를 활성화하지 않는다. 인증 모드를 임의로 JWT로 바꾸지 않는다.
- 사용자가 이 금지를 명시적으로 철회하기 전까지 후속 작업자도 동일하게 준수한다. 원본 인증 actor와 정식 등록 승인 검사는 유지한다.

- '코드'는 알림 문구 식별번호다. 소스코드 분석 도구로 바꾸지 않는다.
- 전체 원본은 upstream.lock.json에 고정되어 있다. 검증 전 다운로드 파일을 신뢰하지 않는다.
- 원본 전체 기능·의존성·로그인·화면을 경량 대체물로 바꾸지 않는다. RRF 하나만 가져와 전체 RAG라고 말하지 않는다.
- 확장은 extensions/, 원본 변경은 deploy/patch_*에 명시하고 고정 소스 계약이 달라지면 빌드를 중단한다.
- 일반 문서 경로를 보존한다. 알림 카탈로그는 원본 엔진에 별도 데이터 스코프를 연결한다.
- 번호·문구는 DB 원문을 출력한다. AI가 등록된 번호나 기존 원문을 만들지 않는다.
- 검색/비교/초안 작업을 대화로 연결하되 정식 등록은 관리자 명시적 승인 엔드포인트로만 한다.
- 승인·변경은 최신 목록/초안 버전·중복 확인·사유·사용자 ID를 검사한다.
- import 미리보기 없이 원문을 덮어쓰거나 목록에서 빠졌다고 자동 삭제하지 않는다.
- 모든 요청의 actor는 원본 get_current_user에서 얻는다. body의 사용자 ID나 관리자 role을 믿지 않는다.
- 회사 데이터와 합성 테스트 자료를 섞지 않는다. production 초기 목록은 비어 있어야 한다.
- 인덱스 미준비/실패를 의미 검색 성공으로 가장하지 않는다. 공개된 최신 스냅샷만 검색한다.
- .env/API 키/토큰/운영 DB/회사 엑셀/다운로드 모델 파일을 ZIP이나 Git에 넣지 않는다.
- integration_tests/의 대역과 오프라인 브리지는 검사에만 사용한다. 운영 fallback으로 넣지 않는다.
- Python 도메인/API 검사, 설치 unittest, JS/TSX 구문, native HTTP, UI 검사를 재실행하고 결과를 구분한다.
- 로컬 API TestClient와 모의 LLM을 사용한 검사를 실제 PGVector/Nori/JWT/모델/Next E2E로 표현하지 않는다.
- 현재 사용자 Docker Local에는 복사·기동하지 않았다. 실제 경로와 권한을 확인하기 전 배포했다고 말하지 않는다.
